main { ] := until > : main /* < , "Hola % <= := } } = == := "Hola" "Hola" != "Hola" read Hola" /* > if Hola" integer { > f Hola" == read ] ] // [ float float [ if ! else end else > a */ = ) <= , read = // ] then if < ! g while >= b d -- then [ > := // + float [ write { repeat "Hola" : a } d - g do , <= // while ! { [ < b then g if ) do == read <= */ [ repeat / - <= until repeat <= bool g - a [ ] { % { // ! { else f := -- a ! % ! < ++}
